version: '3.0'
services:
  streetmerchant:
    image: ghcr.io/jef/streetmerchant:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maximo1491/pokelerts
    env_file:
      - dotenv
